Exercicis Bàsics (C#)
Composició seqüencial
========================

---

!!! note "Ex01"

    #### 1\. Interpreta el significat del següent fragment de codi:

    ```csharp
    int n = Convert.ToInt32(Console.ReadLine( ) ) ;
    n= n + 1 ;
    Console.WriteLine($”HAS ENTRAT : {n-1} I UN MÉS  VAL {n}”);
    Console.WriteLine($”HAS ENTRAT : {n-1:0000} I UN MÉS  VAL {n:0000}”);
    ```

    **Informació extra:**

    - `Convert.ToInt32(string)` converteix una cadena introduïda per teclat a enter.
    - `{n:0000}` mostra el número amb format de 4 dígits (afegint zeros a l'esquerra).
    - El programa primer incrementa el valor introduït i després el mostra de dues maneres.

    **Exemple**
    Entrada: `7`
    Sortida:

    ```csharp
    HAS ENTRAT : 7 I UN MÉS VAL 8
    HAS ENTRAT : 0007 I UN MÉS VAL 0008
    ```

---

!!! note "Ex02"

    #### 2\. Escriu un programa que et demani la teva edat i et digui quants anys faràs l'any vinent.

    **Informació extra:**

    - Operació necessària: `edatVin = edat + 1`
    - Es tracta de fer una suma simple sobre el nombre entrat.

    **Exemple**
    Entrada: `19`
    Sortida: `L'any vinent tindràs 20 anys.`

---

!!! note "Ex03"

    #### 3\. Escriu un programa demani a l’usuari una quantitat d'hores, minuts i segons i ens informi per la consola del total de temps entrat, convertit tot en segons.

    **Informació extra:**

    - Fórmula general:
    ```
    totalSegons = hores * 3600 + minuts * 60 + segons
    ```

    **Exemple**
    Entrada: `2 hores, 5 minuts, 10 segons`
    Sortida: `7510 segons`

---

!!! note "Ex04"

    #### 4\. Escriu un programa que demani per teclat dos valors corresponents als coeficients a i b d’una equació de primer grau ( ax + b = 0) i ens calculi la solució de l’equació. El programa després de demanar els dos valors, ens ha de mostrar l’equació i la solució mostrant els coeficients a i b així com la solució exactament amb dues xifres decimals. No cal considerar el cas a=0;

    **Informació extra:**

    - Fórmula de resolució:
    ```
    x = -b / a
    ```
    - Format decimal C#: `{valor:F2}` → mostra dos decimals.
    - També podem usar: `Math.Round(valor, 2)` → arrodonir a 2 decimals.
    - Encara no ho sabem però en aquest exercici tenim un problema amb el cas `a = 0`.

    **Exemple**
    Entrada: `a = 2`, `b = -6`
    Sortida: `Equació: 2x - 6 = 0 → Solució: 3,00`

---

!!! note "Ex05"

    #### 5\. Escriu un programa per demanar una lletra pel teclat i mostrar el seu codi ASCII a la pantalla.

    **Informació extra:**

    - En C#, podem convertir un `char` a enter:
    ```csharp
    int codi = (int)lletra;
    ```
    - En C# si guardem un char en una variable de tipus `int`, es fa la conversió automàtica.

    **Exemple**
    Entrada: `A`
    Sortida: `65`

---

!!! note "Ex06"

    #### 6\. Escriu un programa per demanar una lletra minúscula i mostrar la lletra majúscula equivalent.

    **Informació extra:**

    - Això implica conversió de caràcter, no un canvi numèric.
    - C# te les funcions: `char.ToUpper(lletra)` i `char.ToLower(lletra)`.
    - Però també es pot fer amb la diferència entre codis ASCII:
    ```csharp
    char lletraMaj = (char)(lletraMin - 32);
    ```

    **Exemple**
    Entrada: `f`
    Sortida: `F`

---

!!! note "Ex07"

    #### 7\. Escriu un programa per mostrar el resultat del residu i la divisió entera de 2 números enters.

    **Informació extra:**

    - Divisió entera en C#: `a / b`
    - Residu en C#: `a % b`

    **Exemple**
    Entrada: `17` i `5`
    Sortida:

    ```
    Divisió entera = 3
    Residu = 2
    ```

---

!!! note "Ex08"  

    #### 8\. Escriu un programa per convertir "Dòlars" a "Euros". El programa ens ha de demanar quant Euros són un dólar, i la quantitat de dòlars que volem convertir. Feu que es mostri el símbol $ i el símbol € correctament.

    **Informació extra:**

    - Fórmula:
    ```
    euros = dolares * valorEuroPerDolar
    ```
    - Per mostrar els símbols € correctament en C#, afegim al principi del programa:
    ```csharp
    Console.OutputEncoding = System.Text.Encoding.UTF8;
    ```

    **Exemple**
    Entrada: `1$ = 0,92€ i 150$`
    Sortida: `150$ = 138€`

---

!!! note "Ex09"  

    #### 9\. Escriu un programa per convertir una quantitat de polzades a metres. Cal tenir en compte que una polzada són 2,54 cm.

    **Informació extra:**

    - Conversió:
    ```
    metres = polzades * 0.0254
    ```

    **Exemple**
    Entrada: `100 polzades`
    Sortida: `2,54 m`

---

!!! note "Ex10"

    #### 10\. L'altitud dels avions es mesura en peus. 1 metre són 39,27 polzades i 1 peu són 12 polzades. 1 polzada són 2,54 cm. Escriu un programa que ens demani una quantitat de peus i ens informi del seu equivalent en metres.

    **Informació extra:**

    - Conversió completa:
    ```
    1 peu = 12 polzades = 12 * 2,54 cm = 30,48 cm = 0,3048 m
    metres = peus * 0.3048
    ```

    **Exemple**
    Entrada: `50 peus`
    Sortida: `15,24 metres`

---

!!! note "Ex11"

    #### 11\. Escriu un programa per calcular la nòmina d'un treballador:

    L’usuari ens ha d’entrar el nombre total d'hores treballades, el sou brut per cada hora treballada i el percentatge d'impost aplicat al sou brut total, el programa ha d'informar de:
    ● Sou brut total
    ● Retenció d’impostos
    ● Sou net (sou brut menys els impostos)

    **Informació extra:**

    - Fórmules:
    ```
    souBrut = hores * preuHora
    retencio = souBrut * percentatge / 100
    souNet = souBrut - retencio
    ```
    - Si volem calcular el sou brut per mes i et donen les hores setmanals, cal multiplicar per `4.3` (setmanes/mes).

    **Exemple**
    Entrada: `40h, 15€/h, 16%`
    Sortida:

    ```
    Sou brut = 600€
    Impostos = 96€
    Sou net = 504€
    ```

---

!!! note "Ex12"

    #### 12\. Resumeix en una frase (en català) l'objectiu d'aquest programa. Abans d'escriure la resposta, escriu la traça donant els valors 2 i 3 a les variables a i b quan l'usuari introdueix les dades.

    ```csharp
    int a, b, c;
    a = Convert.ToInt32(Console.ReadLine());
    b = Convert.ToInt32(Console.ReadLine());
    c = a;
    a = b;
    b = c;
    Console.WriteLine(a);
    Console.WriteLine(b);
    ```

    **Informació extra:**
    Aquest programa intercanvia els valors de dues variables utilitzant una variable auxiliar.
    En classe férem el mateix però vam dir `aux` a la variable auxiliar en lloc de `c`.

    **Traça amb 2 i 3:**

    ```
    a = 2, b = 3, c = ?, després c = 2, a = 3, b = 2
    ```

    **Frase objectiu:**

    > Intercanviar els valors de dues variables.

---

!!! note "Ex13"

    #### 13\. Demana a l'usuari que introdueixi un número enter que representi un any i mostra els 4 dígits per separat.

    **Informació extra:**

    - Es poden obtenir dígits amb divisions i mòduls o convertint a string.
    - Les regles de divisió i mòdul `10` són:

      ```
      valor1 = 1234 % 10 → unitats de 1234 → 4
      valor2 = 1234 / 10 → treiem les unitats de 1234 → 123
      ```

    - Fórmules amb mòduls i divisions:
      ```
      milers = any / 1000
      centenes = (any % 1000) / 100
      desenes = (any % 100) / 10
      unitats = any % 10
      ```

    **Exemple**
    Entrada: `2025`
    Sortida: `2 0 2 5`

---

!!! note "Ex14"

    #### 14\. Escriu un programa per mostrar l'hora en format hh:mm:ss a partir d'una quantitat de segons donada com a entrada.

    Per exemple, si l'entrada és 4000, la sortida ha de ser 01:06:40.

    **Informació extra:**

    - Fórmules:
      ```
      hores = segonsTotal / 3600
      minuts = (segonsTotal % 3600) / 60
      segons = segonsTotal % 60
      ```
      - Explicar a classe una altra forma de calcular minuts:
        ```
        minuts = (segonsTotal - hores * 3600) / 60
        ```
    - Format amb dos dígits:
      ```csharp
      Console.WriteLine($"{hores:00}:{minuts:00}:{segons:00}");
      ```
      **Exemple**
      Entrada: `4000`
      Sortida: `01:06:40`

---

!!! note "Ex15"

    #### 15\. Calcula el canvi d'una quantitat en euros utilitzant la quantitat mínima de monedes. El comerç no accepta bitllets de 500€ i 200€. Has de començar amb bitllets de 100€, després 50€, 20€, 10€, 5€, 2€, 1€, 50 cèntims, 20 cèntims, 10 cèntims, 5 cèntims, 2 cèntims i 1 cèntim. Suposa que el comerç té canvi il·limitat disponible utilitzant bitllets i monedes il·limitats.

    **Informació extra:**

    - Cal treballar en **cèntims** per evitar errors decimals.
    - Estratègia: dividir successivament i calcular residu.

    **Exemple simplificat per 87,63€:**

    ```
    50€ → 1
    20€ → 1
    10€ → 1
    5€ → 1
    2€ → 1
    1€ → 0
    50cts → 1
    10cts → 1
    2cts → 1
    1ct → 1
    ```

---

!!! note "Ex16"

    #### 16\. Escriu un programa que ens mostri el resultat aleatori del partit València - Girona de forma que tant el valor dels gols d’un com de l’altre equip sigui un valor a l’atzar entre 0 i 6.

    **Informació extra:**

    - C#:
    ```csharp
    Random rnd = new Random();
    int golsVal = rnd.Next(0,7);
    int golsGir = rnd.Next(0,7);
    ```
    - On `rnd.Next(0,7)` genera un número aleatori entre 0 i 6, se inclou el 0 però no el 7.

    **Exemple**
    Sortida: `Valencia 4 - 2 Girona`

---

!!! note "Ex17"

    #### 17\. Escriu un programa que et demani el nombre de cares d'un dau i després llença el dau aleatòriament (suposa que el dau està numerat començant per 1).

    **Informació extra:**
    `r.Next(1, n+1)`
    **Exemple**
    Entrada: `10`
    Sortida: `Ha sortit 7`

---

!!! note "Ex18"

    #### 18\. Escriu un programa per mostrar aleatòriament una lletra majúscula.

    **Informació extra:**

    - Lletres majúscules ASCII van de 65 a 90.
    - Exemple C#:
    ```csharp
    char lletra = (char)r.Next(65, 91);
    ```

    **Exemple**
    Sortida: `Q`

---

# 🟩 Exercicis Extra

### 19\. Fer un programa que et demani el nom i et saludi.

**Informació extra:**

- Llegir cadena amb `Console.ReadLine()`
- Mostrar missatge concatenat.

**Exemple:**  
Entrada: `Marc`  
Sortida: `Hola Marc`

---

### 20\. Càlcul del volum d’una esfera a partir del radi

Fórmula:
$\frac{4}{3} \pi r^3$ → `V = 4/3·π·r³`

**Informació extra:**

- Fórmula matemàtica clara per a C#:
  ```
  V = 4.0 / 3 * Math.PI * Math.Pow(r, 3);
  ```

**Exemple:**  
Entrada: `r = 5`  
Sortida: `523,6`

---

### 21\. Fer un programa que passi de pessetes a euros ( 1€ = 166’386 ptes)

**Informació extra:**  
Fórmula:

```
euros = pessetes / 166.386
```

**Exemple:**  
`1000 ptes = 6,01€`

---

### 22\. Fer un programa que passi d’euros a pessetes ( 1€ = 166’386 ptes)

**Informació extra:**

```
pessetes = euros * 166.386
```

**Exemple:**  
`6€ = 998,3 ptes`

---

### 23\. Fer un programa que passi de talles americanes a catalanes. Podem deduir que els americans fan servir la mida de la cintura en polzades (1 polzada = 2,54 cm). La talla dels catalans és la meitat de la mesura de la cintura en cm.

**Informació extra:**

- 1 polzada = 2,54 cm
- Talla catalana = (cintura cm) / 2

**Exemple:**  
`32 polzades → 81,28 cm → talla 40`

---

### 24\. Fes el següent programa invers a l’anterior

**Informació extra:**

- C#: `polzades = (tallaCatalana * 2) / 2.54`

---

### 25\. Fer un programa que passi de graus Celsius a graus Fahrenheit. ( 0°C = 32°F; T(°F) = T(°C) \* 9/5 + 32 )

Fórmula:
$T(°F) = T(°C) \times \frac{9}{5} + 32$ → `T(°F) = T(°C) · 9/5 + 32`

**Informació extra (fórmula):**

- Fórmula matemàtica clara per a C#:
  ```
  F = C * 9.0/5 + 32
  ```

---

### 26\. Fes el programa invers a l’anterior

**Informació extra:**

```
C = (F - 32) * 5.0/9
```

---

### 27\. Fer un programa per calcular el canvi a tornar, fent servir el sistema monetari de pessetes. Per tornar canvi cal saber l’import que hem de cobrar i l’import que ens donen. El canvia a tornar ha de dir quantes monedes de 100, quantes de 50, 25, 5, 1. 17. Tornar el canvi d’una quantitat en € amb el mínim de monedes possibles (suposem que el caixer disposarà sempre de canvi il·limitat). Ha de dir quantes monedes de 2 euros, 1 euro, 50 cts, 20 cts, 10 cts, 5 cts, 2 cts, 1ct. Per fer-ho, una forma seria treballar amb cèntims.

**Informació extra:**  
Treballar amb divisió i residu successiu.

---

### 28\. Què fa el programa? Constata-ho provant-lo amb C#.

```csharp
a = a*b;
b = a/b;
a = a/b;
```

**Informació extra:**

- Intercanvi sense variable auxiliar utilitzant producte i divisió.

---

### 29\. Fes un programa que calculi les solucions d’una equació de segon grau.

Formula general:
$ax^2 + bx + c = 0$

$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$

**Informació extra:**

- Fórmula per a c#:

```
x = (-b ± sqrt(b*b - 4*a*c)) / (2*a)
```

---

### 30\. Simular una màquina d’un parking. L’entrada que tenim seran quatre valors enters (HoraEntrada, MinutEntrada, HoraSortida, MinutSortida) i ha de dir l’import que ha de pagar, tenint en compte que cobrem 3 cèntims per minut (pagament per minut, ja ho hem aconseguit!) i que l’hora de sortida i d’entrada estan dins el mateix dia. El preu has d’indicar-lo en euros.

**Informació extra:**

- Convertir tot a minuts.
- Preu/minut = 0,03€
- Formula:

```
minuts = (horaSortida * 60 + minutSortida) - (horaEntrada * 60 + minutEntrada)
preu = minuts * 0.03
```
